[project]
name = "rsmetacheck-dashboard"
version = "0.2.0"
description = "Landing page to host rsmetacheck-bot monitoring information"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pystache>=0.6.8",
    "rsmetacheck-bot>=0.6.0",
]
